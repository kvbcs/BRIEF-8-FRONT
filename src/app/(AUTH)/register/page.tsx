import { RegisterForm } from "@/Components/Forms/(AUTH)/RegisterForm";
import React from "react";

const page = () => {
	return (
		<main className="w-full h-fit md:min-h-screen md:max-h-fit mb-20">
			<RegisterForm />
		</main>
	);
};

export default page;
